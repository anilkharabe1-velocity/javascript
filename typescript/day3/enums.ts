enum Role {
    ADMIN = 'admin',
    USER = 'user',
    GUEST = 'guest'
}

const role: Role = Role.ADMIN;
console.log("role", role)

enum OrderStatus {
    PENDING = "pending",
    CONFIRMED = 'confirmed',
    DELIVERED = "delivered",
    CANCELLED = "cancelled",
    TRANSIT = 'transit'
}

// 11 sept
let status_: OrderStatus = OrderStatus.CONFIRMED;

// 12 sept
