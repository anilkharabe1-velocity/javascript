const square = (number)=>{
    return number * number;
}


// syntax 1
const square = (number) => number * number;

//syntax 2
const square = number => number * number;

// synstax 3
const powerOf = (number, power) => number ** power;


console.log("powerOf:", powerOf(5, 4));